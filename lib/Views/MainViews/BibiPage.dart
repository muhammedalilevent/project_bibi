import 'package:flutter/material.dart';


class BibiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(
        child: Icon(
          Icons.format_bold,size: 75,
        ),
      ),
    );
  }
}
